# This project targets the browser only. These packages expose optional
# React Native / Expo peer integrations, but npm would otherwise install their
# native toolchain even though the web build never uses it.
legacy-peer-deps=true
